# language: zh-CN
功能: ETO 审核副驾
  作为现场经验入图审核台的 ETO 审核人
  我需要一个坐在审核台里的副驾在我下结论前补足上下文并指出信息错配
  以降低漏判与误判,但副驾永不替我裁决

  背景:
    假如 审核台已加载一条待审核候选现场经验
    而且 副驾只读 ETO 已审核进图的 shared/aggregate 图谱上下文

  场景: 副驾对待审切片产出研判但不自动裁决
    当 ETO 打开一条候选现场经验
    那么 副驾必须给出补足上下文与异议清单
    而且 副驾可以给出建议方向或不表态
    而且 副驾不得自动改变该候选的当前审核状态
    而且 最终审核状态只能由 ETO 提交结论后改变

  场景: 每条异议必须可追溯,否则不得出现
    当 副驾产出一条异议
    那么 该异议必须带 node_ids 或 edge_ids 或 source_refs
    而且 缺少 trace 的异议必须被丢弃,不得呈现给 ETO
    而且 引用法条的异议其 trace 必须落在本次图谱上下文已包含的法条节点上

  场景: 管理经验被法律化必须拦截
    假如 一条候选的法律依据状态是 "no_legal_basis"
    当 候选文本把现场建议写成 "违反" 或 "违法" 或 "不符合 XX 法"
    那么 副驾必须给出严重度为 "blocking" 的异议 "管理经验被法律化"
    而且 建议修正必须是改写为管理建议

  场景: 法条状态有风险必须标注
    假如 一条候选绑定的法条 effective_status 是 "deprecated" 或 "superseded" 或 "pending"
    当 副驾研判该候选
    那么 副驾必须给出异议 "法条状态有风险"
    而且 必须在补足里显示该法条的现状与沿革取代关系

  场景: 证据不足以支撑结论必须标注
    假如 候选的证据摘要缺少该问题类型应有的证据项
    当 副驾研判该候选
    那么 副驾必须给出异议 "证据不足"
    而且 补足里必须列出该问题类型的证据应有项

  场景: 语义重复必须给出合并目标
    假如 候选与图谱已有问题类型语义重复
    当 副驾研判该候选
    那么 副驾必须给出异议 "重复可合并"
    而且 建议修正必须包含图谱已有的合并目标问题类型

  场景: 个案不得被误导进入聚合
    假如 候选的跨企业样本企业数小于 5 或表现为个案
    当 副驾研判该候选
    那么 副驾必须给出异议 "聚合误导风险"
    而且 副驾不得建议方向为 "通过，进入聚合候选"

  场景: 法条依据不可用时表达必须降级
    假如 图谱上下文返回 blocked_refs 非空 或 法律依据状态是 "candidate" 或 "disputed"
    当 副驾研判该候选
    那么 副驾的上下文门禁必须是 "partial" 或 "blocked"
    而且 副驾必须写明降级说明
    而且 副驾不得把候选或争议口径写成确定法律依据

  场景: 大模型不可用时退化到确定性研判
    假如 LLM 或 RAG 不可用
    当 副驾研判该候选
    那么 副驾必须只返回确定性 backbone 的补足与异议
    而且 副驾不得沉默或报错中断审核台
    而且 涉及法条语义的异议必须降级为需人工复核

  场景: RAG 可用时法条原文进 prompt 但不进副驾意见与报告
    假如 RAG 法条原文检索可用且命中本次已审核来源
    当 副驾发起语义研判
    那么 法条原文只进入发往大模型的提示词供研判
    而且 副驾意见与导出报告都不得包含法条原文
    而且 大模型把法条原文回贴进异议时必须被剥离
    而且 仅引用法条条款定位的异议必须保留
    而且 涉及法条适用性的异议不得再降级为需人工复核

  场景: 副驾意见私有零泄漏
    当 副驾返回一条副驾意见
    那么 输出必须通过红线零泄漏扫描
    而且 不得包含企业名、企业 ID、GPS、照片路径、附件路径、法条全文或密钥
    而且 副驾产物不得进入 aggregate 或 shared 导出

  场景: ETO 否决副驾建议时记录分歧
    假如 副驾给出建议方向且 ETO 提交了不同的最终结论
    当 ETO 提交审核结论并带副驾回执
    那么 系统必须生成一条 "ai_review_delta" 治理候选
    而且 该候选默认 review_status 为 "candidate"
    而且 该候选不得自动晋级 approved

  场景: 副驾必须使用中文业务话术
    当 ETO 查看副驾研判
    那么 错配码必须以中文标签呈现
    而且 页面不得展示 "copilot.v1"、"node_ids"、"legal_basis_status"、"machine_gate_status" 等机器码
    而且 机器码只在 "查看溯源" 展开时露出
